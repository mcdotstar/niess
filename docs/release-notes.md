# Release notes

What changed in each version, and what to do about it. This page is the repository's
[`CHANGELOG.md`](https://github.com/mcdotstar/niess/blob/main/CHANGELOG.md), included here
rather than written twice.

Each release on GitHub carries the same notes, and installing a version pins you to what it
says: `pip install 'niess==0.7.0'`.

--8<-- "CHANGELOG.md:releases"
